name: desktop_with_rust
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  ffigen: ^4.1.3
  flutter_rust_bridge: ^1.37.2
  freezed: ^2.0.4
  freezed_annotation: ^2.1.0
  ffi: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  build_runner: ^2.2.0

flutter:
  uses-material-design: true
